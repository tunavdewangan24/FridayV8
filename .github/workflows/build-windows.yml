name: Build FridayV8 Windows EXE

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build EXE
        run: |
          pyinstaller --onefile --name FridayV8 --console friday.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: FridayV8-Windows-EXE
          path: dist/FridayV8.exe
